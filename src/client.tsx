import React from "react";

// Minimal client entry used by the build pipeline.
// This is intentionally a no-op render. Add client-side hydration here
// if your app needs React client components in the future.

export default function ClientEntry() {
  return null;
}
